# Banco de dados Postgres (Supabase)
# Pegue as URLs em: Supabase → Project Settings → Database → Connection string
#
# DATABASE_URL = conexão "Transaction pooler" (porta 6543) usada pela aplicação.
# DIRECT_URL   = conexão direta (porta 5432) usada por migrações / db push / seed.
DATABASE_URL="postgresql://postgres.SEU_REF:SUA_SENHA@aws-0-REGIAO.pooler.supabase.com:6543/postgres?pgbouncer=true&connection_limit=1"
DIRECT_URL="postgresql://postgres.SEU_REF:SUA_SENHA@aws-0-REGIAO.pooler.supabase.com:5432/postgres"

# Supabase Realtime (tempo real no navegador).
# Pegue em: Supabase → Project Settings → API.
# Sem estas variáveis, o tempo real usa o fallback local via SSE.
NEXT_PUBLIC_SUPABASE_URL="https://SEU_REF.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="sua-anon-key-publica"

# Chave da API da Anthropic (Claude) para as funcionalidades de IA.
# Obtenha em https://console.anthropic.com/ . Sem ela, a IA usa um fallback local.
ANTHROPIC_API_KEY=""
